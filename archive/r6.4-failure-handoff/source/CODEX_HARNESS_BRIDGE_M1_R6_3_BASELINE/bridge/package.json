{
  "name": "@local/codex-harness-bridge",
  "version": "0.6.3",
  "private": true,
  "type": "module",
  "description": "Codex-controlled adaptive task decomposition with split-memory learning, parallel minimal Harness execution, token-only budget gates, progressive tool disclosure, llama.cpp fallback, and CNY monitoring.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/*.test.js",
    "doctor": "node dist/doctor-client.js",
    "acceptance": "node dist/acceptance-client.js",
    "direct-acceptance": "node dist/direct-acceptance.js",
    "check": "npm run clean && npm run build && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "22.15.0",
    "typescript": "5.8.3"
  }
}
